# 部门常见问题知识库 - 需求说明文档 (Spec)

## 一、项目概述

### 1.1 项目名称

部门常见问题知识库（Department FAQ Knowledge Base）

### 1.2 项目目标

构建一个现代化的部门内部知识库问答系统，支持员工快速查询部门常见问题，包括报销流程、系统权限、请假流程、办公规范等，并在知识库无法覆盖时提供AI兜底回答。

### 1.3 目标用户

- 部门新员工：快速了解公司/部门各项规章制度
- 老员工：便捷查询不熟悉的流程细节
- 管理员：维护和更新知识库内容

---

## 二、技术栈

### 2.1 后端

- **运行环境**: Node.js >= 18
- **Web框架**: Express.js
- **数据库**: SQLite（better-sqlite3）
- **文件上传**: Multer
- **AI接口**: 支持对接主流大模型API（可配置）

### 2.2 前端

- **UI框架**: 原生HTML/CSS/JS + Tailwind CSS (CDN)
- **图标库**: Font Awesome (CDN)
- **响应式**: 支持PC和移动端

### 2.3 开发工具

- **包管理**: npm
- **启动命令**: `npm install && npm start`

---

## 三、功能需求

### 3.1 知识条目管理（CRUD）

| 功能   | 描述                | 优先级 |
| ---- | ----------------- | --- |
| 新增条目 | 添加问题、答案、分类、关键词    | 高   |
| 编辑条目 | 修改已有知识条目内容        | 高   |
| 删除条目 | 删除指定知识条目          | 高   |
| 查询条目 | 支持按分类、关键词筛选       | 高   |
| 批量导入 | 支持Markdown/文本文件导入 | 中   |

### 3.2 智能问答

| 功能    | 描述                | 优先级 |
| ----- | ----------------- | --- |
| 关键词检索 | 基于关键词匹配知识库条目      | 高   |
| 模糊匹配  | 支持部分匹配和同义词扩展      | 高   |
| 相似度排序 | 按匹配度排序返回结果        | 高   |
| AI兜底  | 知识库无匹配时调用AI生成答案   | 高   |
| 来源标注  | 清晰区分"知识库"vs"AI生成" | 高   |

### 3.3 分类管理

| 功能    | 描述           | 优先级 |
| ----- | ------------ | --- |
| 预设分类  | 报销、请假、权限、办公等 | 高   |
| 自定义分类 | 支持添加/编辑/删除分类 | 中   |
| 分类筛选  | 列表页按分类筛选展示   | 高   |

### 3.4 AI配置

| 功能    | 描述                  | 优先级 |
| ----- | ------------------- | --- |
| 模型选择  | 支持配置不同AI模型          | 中   |
| API配置 | 可配置API Key和Endpoint | 中   |
| 模拟模式  | 无API时启用模拟生成逻辑       | 高   |
| 连接测试  | 测试AI服务可用性           | 中   |

### 3.5 异常处理

| 场景      | 处理方式               |
| ------- | ------------------ |
| AI服务不可用 | 提示"AI服务暂不可用，请稍后重试" |
| 网络异常    | 显示网络错误提示           |
| 数据为空    | 友好的空状态页面           |
| 操作失败    | Toast提示具体错误信息      |

---

## 四、数据库设计

### 4.1 知识条目表 (knowledge_items)

| 字段          | 类型                  | 描述          |
| ----------- | ------------------- | ----------- |
| id          | INTEGER PRIMARY KEY | 主键          |
| question    | TEXT                | 问题内容        |
| answer      | TEXT                | 答案内容        |
| category_id | INTEGER             | 分类ID（外键）    |
| keywords    | TEXT                | 关键词（JSON数组） |
| created_at  | DATETIME            | 创建时间        |
| updated_at  | DATETIME            | 更新时间        |
| is_active   | INTEGER             | 是否启用(1/0)   |

### 4.2 分类表 (categories)

| 字段         | 类型                  | 描述   |
| ---------- | ------------------- | ---- |
| id         | INTEGER PRIMARY KEY | 主键   |
| name       | TEXT                | 分类名称 |
| icon       | TEXT                | 图标名称 |
| sort_order | INTEGER             | 排序权重 |

### 4.3 查询日志表 (query_logs)

| 字段         | 类型                  | 描述               |
| ---------- | ------------------- | ---------------- |
| id         | INTEGER PRIMARY KEY | 主键               |
| query      | TEXT                | 查询内容             |
| source     | TEXT                | 来源(knowledge/ai) |
| result_id  | INTEGER             | 匹配条目ID或NULL      |
| created_at | DATETIME            | 查询时间             |

### 4.4 AI配置表 (ai_config)

| 字段         | 类型                  | 描述    |
| ---------- | ------------------- | ----- |
| id         | INTEGER PRIMARY KEY | 主键    |
| provider   | TEXT                | 服务提供商 |
| api_key    | TEXT                | API密钥 |
| base_url   | TEXT                | API地址 |
| model      | TEXT                | 模型名称  |
| is_enabled | INTEGER             | 是否启用  |
| updated_at | DATETIME            | 更新时间  |

---

## 五、API接口设计

### 5.1 知识条目接口

```
GET    /api/knowledge          # 获取知识条目列表（支持筛选、分页）
POST   /api/knowledge          # 新增知识条目
PUT    /api/knowledge/:id      # 更新知识条目
DELETE /api/knowledge/:id      # 删除知识条目
POST   /api/knowledge/import  # 批量导入Markdown/文本
```

### 5.2 分类接口

```
GET    /api/categories         # 获取分类列表
POST   /api/categories         # 新增分类
PUT    /api/categories/:id     # 更新分类
DELETE /api/categories/:id     # 删除分类
```

### 5.3 问答接口

```
POST   /api/query             # 智能问答（核心接口）
```

请求体：

```json
{
  "question": "如何申请报销？"
}
```

响应体：

```json
{
  "source": "knowledge",  // knowledge | ai
  "answer": "报销流程如下：1. 填写报销单...",
  "matched_item_id": 1,
  "matched_question": "如何申请报销？",
  "similarity": 0.95,
  "confidence": "high"
}
```

### 5.4 AI配置接口

```
GET    /api/ai/config         # 获取AI配置
PUT    /api/ai/config         # 更新AI配置
POST   /api/ai/test           # 测试AI连接
```

---

## 六、前端页面设计

### 6.1 页面结构

| 页面    | 路由        | 功能        |
| ----- | --------- | --------- |
| 首页/问答 | /         | 智能问答交互界面  |
| 知识库列表 | /list     | 全部条目展示与筛选 |
| 条目管理  | /admin    | 增删改查操作    |
| AI配置  | /settings | AI服务配置    |

### 6.2 UI组件

1. **问答对话框**
   
   - 输入框：支持多行输入
   - 发送按钮：点击或回车发送
   - 结果展示：区分"知识库"和"AI生成"标签

2. **来源标签 (Badge)**
   
   - 知识库来源：绿色 Badge
   - AI生成来源：蓝色/紫色 Badge

3. **分类筛选器**
   
   - 下拉选择或标签切换
   - 支持多选

4. **管理表单**
   
   - 问题、答案、分类、关键词
   - 富文本编辑（基础格式）

### 6.3 交互流程

```
用户提问 → 前端发送到后端 → 后端关键词匹配知识库
                                    ↓
                            找到匹配项？ → 是 → 返回知识库答案（绿色标签）
                                    ↓ 否
                            调用AI接口 → 成功 → 返回AI答案（紫色标签）
                                    ↓ 失败
                            返回模拟答案 + 提示"AI暂不可用"
```

---

## 七、文件结构

```
knowledge-base/
├── SPEC.md                    # 需求说明文档（先于代码编写）
├── README.md                  # 项目使用说明
├── package.json               # 项目配置与依赖
├── config.js                  # 全局配置（端口、路径、AI默认配置）
├── server.js                  # 后端入口（Express 启动、静态托管、路由挂载）
├── start.bat                  # Windows 一键启动脚本
├── routes/                    # API 路由层
│   ├── knowledge.js           # 知识条目 CRUD + 文件导入
│   ├── categories.js          # 分类管理
│   ├── query.js               # 智能问答（核心接口）
│   └── aiConfig.js            # AI 配置读取/更新/连接测试
├── services/                  # 业务服务层
│   ├── db.js                  # SQLite 初始化 + 种子数据
│   ├── matcher.js             # 关键词/模糊/编辑距离匹配引擎
│   └── aiService.js           # AI 服务封装（多 provider + 模拟模式）
├── data/
│   └── knowledge.db           # SQLite 数据库（运行时生成）
├── public/                    # 前端单页应用（SPA，多视图切换）
│   ├── index.html             # 单页主界面（问答/列表/管理/配置）
│   ├── css/
│   │   └── style.css          # 自定义样式
│   └── js/
│       ├── api.js             # 前端 API 封装
│       └── app.js             # 页面交互逻辑
└── uploads/                   # 上传文件存储
```

> 注：Spec 初稿规划为多页面（list.html / admin.html / settings.html 分离），实现阶段评估后优化为**单页应用多视图切换**（index.html 内切换），以减少重复加载、提升交互流畅度。此差异说明 Spec 是实现前撰写的规划，而非事后补写。

---

## 八、示例知识库内容

系统将预置以下类型的示例问答：

1. **报销流程类**
   
   - 如何申请差旅报销？
   - 报销需要哪些材料？
   - 报销审批流程是怎样的？

2. **请假流程类**
   
   - 如何申请年假？
   - 病假需要什么证明？
   - 事假如何申请？

3. **系统权限类**
   
   - 如何申请系统账号？
   - 权限过期如何续期？
   - 如何申请管理员权限？

4. **办公规范类**
   
   - 公司上下班时间？
   - 会议室如何预订？
   - 快递收件地址？

5. **IT支持类**
   
   - VPN如何连接？
   - 邮箱如何配置？
   - 打印机如何设置？

---

## 九、验收标准

### 9.1 功能验收

- [x] 能正常启动服务（npm install && npm start）
- [x] 知识条目支持完整CRUD操作
- [x] 文件导入功能正常
- [x] 智能问答能正确匹配知识库
- [x] 无匹配时AI兜底正常
- [x] 来源标签清晰可见
- [x] 分类筛选正常
- [x] AI配置可动态修改
- [x] 异常情况有友好提示

### 9.2 界面验收

- [x] 界面美观、现代
- [x] 响应式布局
- [x] 操作流畅
- [x] 无白屏或明显错误

---

## 十、开发流程与 Trae 模式说明

本严格按"**先写 Spec 再实现**"的路径推进，每一步明确所用 Trae 模式，可逐项追溯。

### 10.1 流程与模式对照表

| 阶段   | 步骤                       | Trae 模式                         | 产出 / 说明                                                   |
| ---- | ------------------------ | ------------------------------- | --------------------------------------------------------- |
| ① 规划 | 需求理解 + Spec 编写           | **Plan（规划模式，只读不写码）**            | 产出本 SPEC.md：技术栈、数据库设计、API 设计、页面设计、验收标准全部先行确定，再进入实现        |
| ② 实现 | 后端骨架（routes/services/db） | **Agent（执行模式）**                 | 按 Spec 的 API 设计生成 server.js / routes/*.js / services/*.js |
| ③ 实现 | 前端单页应用                   | **Agent（执行模式）**                 | 按 Spec 页面设计生成 index.html + api.js + app.js（由多页优化为 SPA）    |
| ④ 验证 | 环境与依赖排查                  | **Agent + 交互（AskUserQuestion）** | 诊断 Node 未安装、better-sqlite3 原生模块编译，交互确认安装方案                |
| ⑤ 验证 | 启动与功能测试                  | **Agent（验证）**                   | `npm start` 启动，脚本验证健康检查 + 4 个提示词召回效果                      |
| ⑥ 迭代 | 三峡知识库录入                  | **Plan → Agent**                | 先读 3 份知识库 md 文档（Plan），再写入 db.js 种子数据（Agent）               |
| ⑦ 迭代 | 跨文档召回算法优化                | **Plan → Agent → Verify 循环**    | 诊断复合问题召回不准 → 设计子问题拆分 + 多文档综合召回 → 4 提示词回归测试                |
| ⑧ 打磨 | 界面友好性增强                  | **Agent（执行）**                   | 空状态提示、错误提示卡片、来源标签颜色区分（知识库绿 / AI 紫 / 未匹配橙）                 |
| ⑨ 调试 | 异常处理                     | **Agent（调试）**                   | 端口占用(EADDRINUSE)、中文路径编码、PowerShell 脚本策略等逐一排查解决            |

### 10.2 "先 Spec 再实现"

1. **时间顺序**：SPEC.md（含技术栈/DB/API/页面/验收标准）在第一份代码文件生成前完成，实现阶段始终以 Spec 为蓝图。
2. **设计演进留痕**：SPEC.md 第七节初稿规划为多页面（list.html / admin.html / settings.html），实现阶段评估后优化为单页应用多视图。此差异被显式记录在 Spec 中，说明 Spec 是实现前的规划而非事后补写。
3. **验收可检查**：第九节验收标准与功能需求一一对应、逐项勾选，可直接用于考核验收。

### 10.3 模式使用要点

- **Plan 模式**用于"想清楚再动手"的环节：需求拆解、Spec 编写、算法优化前的诊断分析——只读探索、不直接改码。
- **Agent 模式**用于"按蓝图落地"的环节：批量生成代码、安装依赖、启动测试、修复缺陷。
- **交互（AskUserQuestion）**用于需要用户决策的分叉点：如 Node.js 安装方式选择。
- 遇到召回不准、端口冲突等问题时，遵循 **诊断 → 假设 → 修复 → 验证** 的闭环，而非盲目试错。