# 系统技术规格说明

> 文档版本：v1.0  
> 对应项目：部门常见问题知识库（Department Knowledge Base）  
> 与 [SPEC.md](./SPEC.md) 的关系：SPEC.md 为需求说明（做什么、验收什么），本文档为技术规格说明（怎么实现、模块如何设计），二者互补。

---

## 一、系统概述

### 1.1 定位

面向部门内部的常见问题问答工具：员工提问 → 优先在本地知识库检索 → 命中则返回知识库答案（标注出处）→ 未命中则调用 AI 生成参考答案（标注 AI 兜底）→ AI 不可用时给出友好提示。答案来源一眼可辨。

### 1.2 设计目标

| 目标    | 实现方式                                               |
| ----- | -------------------------------------------------- |
| 轻量易部署 | Node.js + SQLite，单进程，`npm install && npm start` 即用 |
| 离线可用  | 知识库本地检索；AI 默认模拟模式，无需外部密钥即可演示                       |
| 来源可溯  | 答案带来源 Badge + 出处文档名，区分知识库/AI/未匹配                   |
| 容错友好  | 空数据、AI 不可用、网络异常均有提示，不白屏                            |

---

## 二、系统架构

### 2.1 分层架构

```
┌─────────────────────────────────────────────────┐
│  前端 (public/)  原生 HTML/JS + Tailwind CSS    │
│  单页应用 SPA：问答 / 列表 / 管理 / 配置 四视图  │
└────────────────────┬────────────────────────────┘
                     │ fetch (/api/*)
┌────────────────────▼────────────────────────────┐
│  接入层  server.js (Express)                     │
│  中间件：cors / json / static / multer / 404 / 错误处理 │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│  路由层  routes/                                  │
│  knowledge.js · categories.js · query.js · aiConfig.js │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│  服务层  services/                                │
│  db.js(SQLite) · matcher.js(检索引擎) · aiService.js(AI) │
└────────────────────┬────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────┐
│  存储层  data/knowledge.db (SQLite, WAL)          │
│  uploads/ (上传文件临时存储)                      │
└─────────────────────────────────────────────────┘
```

### 2.2 请求流转（以智能问答为例）

```
前端 askQuestion(question)
  → POST /api/query
  → query.js 路由
  → matcher.executeQuery()  本地检索
       ├─ 命中(score≥0.5) → 返回 source=knowledge（含出处文档、相关条目）
       └─ 未命中 → aiService.getAIAnswer()
                       ├─ AI 成功 → 返回 source=ai
                       └─ AI 失败 → 返回 source=none（友好提示）
```

---

## 三、技术栈

| 层      | 技术                                             | 说明                           |
| ------ | ---------------------------------------------- | ---------------------------- |
| 运行环境   | Node.js ≥ 18                                   | `engines` 约束                 |
| Web 框架 | Express 4.18                                   | 中间件 + 路由                     |
| 数据库    | SQLite (better-sqlite3 11.x)                   | 同步 API，WAL 模式                |
| 文件上传   | Multer 1.4                                     | md/txt 上限 5MB                |
| 跨域     | cors 2.8                                       | 全局开放                         |
| 前端     | 原生 HTML/JS + Tailwind CSS (CDN) + Font Awesome | 无构建步骤                        |
| AI     | 原生 https/http 调用                               | 支持 DeepSeek / 豆包 / 通义千问 / 模拟 |

依赖清单见 [package.json](./package.json)，共 4 个运行时依赖。

---

## 四、目录结构

```
knowledge-base/
├── server.js              # 后端入口：Express 启动、中间件、路由挂载、上传、SPA 回退
├── config.js              # 全局配置：端口、DB 路径、上传限制、AI providers、匹配参数
├── start.bat              # Windows 一键启动脚本
├── routes/
│   ├── knowledge.js       # 知识条目 CRUD + Markdown 导入 + 解析
│   ├── categories.js      # 分类 CRUD（含条目计数、删除保护）
│   ├── query.js           # 智能问答核心（检索→AI兜底→来源标注）
│   └── aiConfig.js        # AI 配置读取/更新/连接测试（API Key 掩码）
├── services/
│   ├── db.js              # SQLite 初始化、建表、种子数据、连接管理
│   ├── matcher.js         # 检索引擎：5 级匹配 + 子问题拆分 + 置信度
│   └── aiService.js       # AI 封装：多 provider、模拟兜底、连接测试
├── data/knowledge.db      # SQLite 数据库（运行时生成）
├── public/
│   ├── index.html         # SPA 单页主界面（四视图切换）
│   ├── css/style.css      # 自定义样式
│   └── js/
│       ├── api.js         # 前端 API 封装（统一 fetch + 错误抛出）
│       └── app.js         # 页面交互逻辑（问答/列表/管理/配置/空状态/错误提示）
└── uploads/               # 上传文件临时存储（导入后即删除）
```

---

## 五、数据库设计

采用 SQLite，启用 `WAL` 模式与 `foreign_keys`。共 4 张表、3 个索引。

### 5.1 表结构

#### categories（分类表）

| 字段         | 类型                       | 说明                        |
| ---------- | ------------------------ | ------------------------- |
| id         | INTEGER PK AUTOINCREMENT | 主键                        |
| name       | TEXT NOT NULL UNIQUE     | 分类名（三峡文档分类名即来源文档名，用于出处标注） |
| icon       | TEXT DEFAULT 'folder'    | 图标                        |
| sort_order | INTEGER DEFAULT 0        | 排序权重                      |
| created_at | DATETIME                 | 创建时间                      |

#### knowledge_items（知识条目表）

| 字段                      | 类型                        | 说明                   |
| ----------------------- | ------------------------- | -------------------- |
| id                      | INTEGER PK AUTOINCREMENT  | 主键                   |
| question                | TEXT NOT NULL             | 问题                   |
| answer                  | TEXT NOT NULL             | 答案（三峡条目内嵌【出处：《文档名》】） |
| category_id             | INTEGER FK→categories(id) | 分类                   |
| keywords                | TEXT DEFAULT '[]'         | 关键词 JSON 数组          |
| is_active               | INTEGER DEFAULT 1         | 启用标志（软删除用）           |
| created_at / updated_at | DATETIME                  | 时间戳                  |

#### query_logs（查询日志表）

| 字段         | 类型         | 说明                    |
| ---------- | ---------- | --------------------- |
| id         | INTEGER PK | 主键                    |
| query      | TEXT       | 查询内容                  |
| source     | TEXT       | knowledge / ai / none |
| result_id  | INTEGER    | 命中条目 ID 或 NULL        |
| created_at | DATETIME   | 查询时间                  |

#### ai_config（AI 配置表）

| 字段         | 类型                  | 说明                              |
| ---------- | ------------------- | ------------------------------- |
| id         | INTEGER PK          | 主键                              |
| provider   | TEXT DEFAULT 'mock' | mock / deepseek / doubao / qwen |
| api_key    | TEXT                | API 密钥                          |
| base_url   | TEXT                | 自定义接口地址                         |
| model      | TEXT                | 模型名                             |
| is_enabled | INTEGER             | 是否启用                            |
| updated_at | DATETIME            | 更新时间                            |

### 5.2 索引

- `idx_knowledge_category` on knowledge_items(category_id)
- `idx_knowledge_active` on knowledge_items(is_active)
- `idx_query_logs_query` on query_logs(query)

### 5.3 种子数据

首次启动且分类为空时自动注入：11 个分类（7 个部门常规 + 3 个三峡文档 + 其他）、19 条知识条目（含三峡白鹤滩/监测巡检/符合性初审三文档共 8 条）。详见 [db.js](./services/db.js)。

---

## 六、核心模块设计

### 6.1 检索引擎 matcher.js

#### 6.1.1 五级匹配策略（按优先级，单条目打分）

| 级别       | 条件                                | 得分                  | matchedType       |
| -------- | --------------------------------- | ------------------- | ----------------- |
| 1 精确匹配   | 问题全等（忽略大小写）                       | 1.0                 | exact             |
| 2 问题包含查询 | question.includes(query)          | 0.85                | question_contains |
| 3 查询包含问题 | query.includes(question)          | 0.75                | query_contains    |
| 4 关键词加权  | 预定义关键词命中×2 + 分词命中×1，除以(词数×2)      | 动态                  | keyword/partial   |
| 5 模糊匹配   | Levenshtein 编辑距离（仅 score<0.3 时启用） | (1-dist/maxLen)×0.6 | partial           |

- 最高分限制为 0.95，避免与精确匹配(1.0)混淆。
- 置信度分级：`high(≥0.8)` / `medium(≥0.5)` / `low(<0.5)`。

#### 6.1.2 复合问题子问题拆分

针对含 ①②③④ 编号或分号的长复合问题（如提示词4），先拆分为子问题，各子问题独立打分取最高分，避免长文本稀释关键词权重。拆分规则：

1. 优先按 `①②③④⑤⑥⑦⑧⑨⑩` 拆分（子串长度>4）
2. 否则按 `；;` 拆分（子串长度>6）
3. 否则不拆分

#### 6.1.3 检索流程

```
searchKnowledge(query, {minScore=0.3, topK=3, categoryId})
  → 查询 is_active=1 的条目（可按分类过滤）
  → 拆分子问题 → 逐条打分（取子问题最高分）
  → 过滤 score≥minScore → 降序 → 截取 topK
```

### 6.2 问答路由 query.js（核心业务）

#### 6.2.1 三段式决策

```
POST /api/query { question }
  1. executeQuery(question, {topK:10})   // 取 top10 支持跨文档综合
  2. 若 hasMatch 且 results[0].score ≥ 0.5：
       → 跨文档综合召回（见 6.2.2）
       → 返回 source=knowledge
  3. 否则调用 AI 兜底：
       → getAIAnswer(question)
       → 成功返回 source=ai（含 is_mock 标识）
       → AI 失败返回 source=none（友好提示）
```

#### 6.2.2 跨文档综合召回

当 `topK=10` 检索结果中存在 ≥2 条 `score≥0.5` 的相关条目时，判定为综合性问题，合并为综合答案：

- 答案格式：`该问题涉及多个知识点，系统从知识库综合召回 N 条相关内容：` + 逐条 `【i】来源：《文档名》问：… 答：…`
- `source_doc` 标注为「多文档综合」
- 同时返回 `related_results`（相关条目）与 `similar_questions`（相似问题，最多3条）

单条命中时则直接返回该条目答案，`source_doc` 为其分类名（即出处文档名）。

### 6.3 AI 服务 aiService.js

#### 6.3.1 多 Provider 支持

| Provider | 接口                                                            | 默认模型           |
| -------- | ------------------------------------------------------------- | -------------- |
| mock     | 内置模拟（无需网络）                                                    | —              |
| deepseek | https://api.deepseek.com/v1/chat/completions                  | deepseek-chat  |
| doubao   | https://ark.cn-beijing.volces.com/api/v3/chat/completions     | doubao-pro-32k |
| qwen     | https://dashscope.aliyuncs.com/.../text-generation/generation | qwen-turbo     |

#### 6.3.2 调用决策

- `is_enabled=false` 或 `provider=mock` 或 `api_key` 为空 → 走模拟模式
- 否则走真实 AI（原生 https，30s 超时）
- 真实 AI 失败 → 自动降级为模拟答案，并在 `message` 中标注原因

#### 6.3.3 模拟兜底

内置 8 类问题（报销/请假/权限/会议室/VPN/邮箱/打印机/福利）的模板答案，按关键词命中匹配；无命中时返回通用引导答案。所有模拟答案均带「AI兜底回答」前缀与提示语。

### 6.4 文件导入 knowledge.js

#### 6.4.1 Markdown 解析规则（parseMarkdownContent）

- `# 标题` → 新条目的问题
- `## 标题` → 条目分类
- `关键词：a,b,c` → 关键词数组
- `- ` / `* ` 列表 → 答案正文
- 其余行 → 答案正文

#### 6.4.2 上传导入（server.js）

- Multer 限制：仅 `.md/.txt/.markdown`，最大 5MB
- 流程：保存→读取→解析→事务批量插入（分类自动查找或新建）→删除临时文件

### 6.5 容错与安全设计

| 场景         | 设计                                     |
| ---------- | -------------------------------------- |
| 知识库删除      | 软删除（is_active=0），不丢数据                  |
| 分类删除保护     | 分类下有条目时禁止删除，返回条目数提示                    |
| API Key 安全 | 返回时掩码（前4+****+后4），更新时若含 `****` 则保留原值   |
| 文件类型       | 白名单过滤 md/txt/markdown                  |
| 全局错误       | 统一错误中间件，500 返回友好 JSON，不抛堆栈             |
| 404        | API 返回 JSON；其余路径回退到 index.html（SPA 支持） |
| AI 失败      | 降级模拟答案 + message 标注；全失败返回 source=none  |
| 前端空数据      | 空状态组件（图标+文案+引导）                        |
| 前端查询出错     | 持久红色错误卡片（错误详情+可能原因+重试按钮）               |
| 前端 AI 兜底失败 | 橙色警告卡片（未匹配+建议）                         |
| 优雅关闭       | SIGINT/SIGTERM 关闭数据库连接                 |

---

## 七、API 接口规范

所有接口前缀 `/api`，JSON 通信，统一响应 `{ success: boolean, ... }`。

### 7.1 智能问答

| 方法   | 路径                 | 说明                  |
| ---- | ------------------ | ------------------- |
| POST | /api/query         | 智能问答（核心）            |
| GET  | /api/query/history | 查询历史（?limit=50）     |
| GET  | /api/query/stats   | 查询统计（总数/知识库命中/AI兜底） |

**POST /api/query**

```json
// 请求
{ "question": "白鹤滩电站的装机容量是多少？" }

// 响应（知识库命中）
{
  "success": true,
  "source": "knowledge",
  "answer": "...",
  "matched_question": "...",
  "matched_item_id": 18,
  "category": "白鹤滩电站参数手册",
  "source_doc": "白鹤滩电站参数手册",
  "confidence": "high",
  "score": 0.95,
  "related_results": [ { ... } ],
  "similar_questions": [ { ... } ]
}

// 响应（AI 兜底）
{ "success": true, "source": "ai", "is_mock": true, "answer": "...", "note": "知识库中未找到相关内容，AI兜底回答" }

// 响应（AI 也失败）
{ "success": false, "source": "none", "answer": "抱歉...", "message": "知识库中未找到...AI服务也暂不可用..." }
```

### 7.2 知识条目

| 方法     | 路径                    | 说明                                       |
| ------ | --------------------- | ---------------------------------------- |
| GET    | /api/knowledge        | 列表（?category_id&search&page&page_size）   |
| GET    | /api/knowledge/:id    | 单条                                       |
| POST   | /api/knowledge        | 新增（question,answer,category_id,keywords） |
| PUT    | /api/knowledge/:id    | 更新（支持部分字段）                               |
| DELETE | /api/knowledge/:id    | 软删除                                      |
| POST   | /api/knowledge/import | 导入 Markdown 文本（{content}）                |
| POST   | /api/knowledge/upload | 上传 md/txt 文件导入（multipart file）           |

### 7.3 分类

| 方法     | 路径                  | 说明                       |
| ------ | ------------------- | ------------------------ |
| GET    | /api/categories     | 列表（含每分类条目数 item_count）   |
| POST   | /api/categories     | 新增（name,icon,sort_order） |
| PUT    | /api/categories/:id | 更新                       |
| DELETE | /api/categories/:id | 删除（有条目时禁止）               |

### 7.4 AI 配置

| 方法   | 路径             | 说明                             |
| ---- | -------------- | ------------------------------ |
| GET  | /api/ai/config | 获取配置（API Key 掩码）+ 可用 providers |
| PUT  | /api/ai/config | 更新配置                           |
| POST | /api/ai/test   | 连接测试                           |

### 7.5 其他

| 方法  | 路径          | 说明                             |
| --- | ----------- | ------------------------------ |
| GET | /api/health | 健康检查（status/version/timestamp） |

---

## 八、前端架构

### 8.1 单页应用（SPA）

- [index.html](./public/index.html) 为唯一入口，内含 4 个视图区块，通过显隐切换：**问答 / 知识列表 / 条目管理 / AI 配置**
- 后端对 `/`、`/list`、`/admin`、`/settings` 均回退到 index.html

### 8.2 模块划分

- [api.js](./public/js/api.js)：封装所有 `/api` 调用，统一 `request()` 处理响应解析与错误抛出
- [app.js](./public/js/app.js)：页面交互、视图渲染、空状态/错误提示、来源 Badge 颜色区分

### 8.3 来源标注（答案来源明显区分）

| 来源    | Badge 样式      | 颜色  | 触发条件             |
| ----- | ------------- | --- | ---------------- |
| 知识库   | 📚 知识库 ·《文档名》 | 绿色  | source=knowledge |
| AI 生成 | 🤖 AI生成       | 紫色  | source=ai        |
| 未匹配   | ⚠️ 未匹配        | 橙色  | source=none      |
| 服务异常  | ⚠️ 服务异常       | 红色  | fetch 失败         |

知识库命中时额外展示「跨文档参考」蓝底卡片，列出相关条目及其出处文档与相关度。

### 8.4 异常提示（无白屏）

| 场景       | 提示形式                                 |
| -------- | ------------------------------------ |
| 列表无数据    | 空状态：📦 图标 + "暂无知识条目" + 引导文案          |
| 列表加载失败   | 错误状态：⚠️ + "列表加载失败" + 错误详情 + 重新加载按钮   |
| 查询失败     | 红色错误卡片：无法获取答案 + 错误详情 + 可能原因 + 重新查询按钮 |
| AI 兜底也失败 | 橙色警告卡片：未找到匹配答案 + 详情 + 建议             |
| 操作反馈     | Toast 轻提示（成功/错误）                     |

---

## 九、配置说明（config.js）

```js
server:   { port: 3000, host: '0.0.0.0' }   // 端口可用 PORT 环境变量覆盖
database: { path: ./data/knowledge.db }
upload:   { dir: ./uploads, maxSize: 5MB }
ai:       { defaultProvider: 'mock', providers: { mock/deepseek/doubao/qwen } }
matching: { minScore: 0.3, topK: 3, fuzzyMatch: true }
```

匹配参数含义：`minScore` 最低返回阈值；`topK` 默认返回条数（问答核心调用时覆盖为 10 以支持跨文档综合召回）；`fuzzyMatch` 是否启用编辑距离模糊匹配。

---

## 十、部署与运行

### 10.1 环境要求

- Node.js ≥ 18
- Windows / macOS / Linux 均可

### 10.2 启动

```bash
npm install      # 安装依赖（better-sqlite3 为原生模块，需编译）
npm start        # 即 node server.js
```

启动后访问 `http://localhost:3000`。Windows 亦可双击 `start.bat`。

### 10.3 停止

`Ctrl+C` 触发 SIGINT，自动关闭数据库连接。

### 10.4 数据位置

- 数据库：`data/knowledge.db`（含 WAL/SHM 辅助文件）
- 上传：`uploads/`（导入后即清理，通常为空）

---

## 十一、关键设计决策

1. **单页应用而非多页**：Spec 初稿规划多页，实现时优化为 SPA 多视图，减少重复加载、提升交互流畅度（已反向记录于 SPEC.md 第七节）。
2. **软删除而非物理删除**：知识条目 `is_active=0`，保留数据可追溯性，符合知识库审计需求。
3. **AI 默认模拟模式**：考核/演示环境无需密钥即可完整跑通问答闭环；配置真实 Key 后自动切换。
4. **跨文档综合召回**：针对三峡业务复合问题（如提示词4的4问），通过 top10 + 多条高相关合并，避免单条 best match 偏离综合意图。
5. **出处文档名即分类名**：三峡三份文档以文档名作为分类名，知识库命中时 `source_doc` 直接来自分类名，实现"可溯源"。
6. **来源标注三层颜色**：绿(知识库)/紫(AI)/橙(未匹配)/红(服务异常)，前端一眼可辨，满足"答案来源明显区分"要求。
